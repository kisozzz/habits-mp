<view class="wrapper vh100">
  <view class="background-top column-center">
    <dashboard-individual info="{{habits}}" />
  </view>

  <view class="mt180 mb300">
    <view class="column-center">
    <block wx:for="{{nextHabits}}" wx:key="index">
      <habit-card info="{{item}}" class="mb20" />
    </block>
    <view wx:if="{{habits.master_habits.length === 0}}" class="text-primary fbody inconsolata">
      Quartz helps you manage the habits that you want to build. Once you create a habit, it will show up here. Each habit is made up of several steps, and you can also easily track your step progress from this page.
    </view>
    </view>
    <navigator url="/pages/create-habit/create-habit" class="flex items-center mx60 mt"> 
      <image class="icon-sizing mr10" src="/images/icons/pixelsphere.svg"></image>
      <view class="text-primary inconsolata fbody">Add a new habit</view> 
    </navigator>
  </view>

  <tabbar info="{{habits}}" />
</view>
<view class="empty-space"></view>
