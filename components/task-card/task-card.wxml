<view class="task-card">
    <view bindtap="checkDone" value="{{info.completed}}" class="cube {{info.completed?'done':''}}"></view>
    <view class="task-name">{{info.name}}</view>
    {{info.completed}}
</view>
